version: "3.9"

services:
  playground:
    build:
      context: .
    container_name: playground
    ports:
      - "10022:22"
    mem_limit: 4g
    cpus: 2
    volumes:
      - playground_home:/home

volumes:
  playground_home:
